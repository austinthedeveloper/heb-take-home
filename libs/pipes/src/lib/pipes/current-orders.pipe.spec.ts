import { CurrentOrdersPipe } from './current-orders.pipe';

describe('CurrentOrdersPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrentOrdersPipe();
    expect(pipe).toBeTruthy();
  });
});
